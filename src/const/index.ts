import path = require('path');

export const ROOT = path.join(__dirname, './../../');

export const inputDir = path.join(ROOT, 'input');
export const outputDir = path.join(ROOT, 'output');
export const CONTAINER = 'META-INF/container.xml';
